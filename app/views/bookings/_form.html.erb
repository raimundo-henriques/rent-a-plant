<%= simple_form_for [plant, booking] do |f| %>
<div class="d-flex justify-content-center">
  <div class="col-8">
    <%= f.input :start_date %>
    <%= f.input :end_date %>
  </div>
</div>
  <div class="d-flex justify-content-center">
    <%= f.button :submit, "Book #{plant.common_name}", class:'btn btn-success my-5' %>
  </div>
<% end %>
